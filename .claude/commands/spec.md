# Spec

Load `.claude/product-spec-from-pdf.txt`, `PROJECT_INDEX.md`, and `src/App.tsx`.

Output a concise implementation brief with:
- current objective
- routes/components/functions affected
- data model or RLS impact
- invariants that must not change
- validation plan (lint/build/manual path)

Keep it code-oriented and specific to this repo.
