Read `.claude/context.md` first.

Then update `.claude/context.md` with:
- active sprint and current story
- what was completed in this session
- files changed (paths only)
- checks run and outcomes
- blockers and assumptions
- exact next action for next session

Rules:
- use concise bullet points
- overwrite stale sections instead of duplicating
- do not auto-commit or auto-push unless explicitly requested
